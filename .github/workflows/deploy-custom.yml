# .github/workflows/deploy-custom.yml
name: Deploy custom
on:
  push:
    branches: [custom]
jobs:
  trigger-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Call Multica deploy webhook
        run: curl -sf -X POST "${{ secrets.MULTICA_DEPLOY_WEBHOOK_URL }}"
